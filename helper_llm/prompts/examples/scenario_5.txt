dialogue: <Commander> point gripper to k points in a diagonal line, where k is the number of bowls
Python script:
bowl_names = [name for name in get_obj_names() if 'bowl' in name]
start_pos = denormalize_xy([0.1, 0.1])
end_pos = denormalize_xy([0.9, 0.9])
points = np.linspace(start=start_pos, stop=end_pos, num=len(bowl_names))
for point in points:
    point_gripper_to(point)
